/* Spikegolf Supabase config
   -----------------------------------------------------------
   Replace the two strings below with the values from
   Project Settings → API in your Supabase dashboard.
   Leave them empty to run the app in local-only mode.
   The anon key is safe to embed in the client — Row Level
   Security in app_state controls what it can actually do.
*/

export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
